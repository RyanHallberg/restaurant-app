# Build-time constants (Vite inlines these at `npm run build`).
# Local dev: leave VITE_API_URL unset — the Vite proxy forwards /api to :8080.
# Prod build: the Cloud Run origin, e.g.
# VITE_API_URL=https://restaurant-api-729631573980.us-central1.run.app
VITE_API_URL=
